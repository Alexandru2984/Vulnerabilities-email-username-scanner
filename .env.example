# Server Settings
PORT=8080
HOST=0.0.0.0

# Database Settings
# ⚠️  Change these credentials for production!
DATABASE_URL=postgres://recon_user:recon_password@localhost:5454/recon_db

# Authentication
# ⚠️  REQUIRED: Generate a strong random key for production!
# Example: openssl rand -hex 32
API_KEY=changeme_generate_a_secure_key

# Logging (optional)
# RUST_LOG=info,antigravity_loose=debug

# Plugin Keys (Optional)
# HIBP_API_KEY=your_key_here
# GITHUB_TOKEN=your_token_here
